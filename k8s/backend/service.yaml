apiVersion: v1
kind: Service
metadata:
  name: backend-service-${COLOR}
  namespace: ${NAMESPACE}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/api/health"
spec:
  selector:
    app: backend-${COLOR}
    color: ${COLOR}
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
