apiVersion: v1
kind: Service
metadata:
  name: frontend-service-${COLOR}-${VERSION}
  namespace: ${NAMESPACE}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: "/"
spec:
  type: ClusterIP
  selector:
    app: frontend-${COLOR}
    color: ${COLOR}
    version: ${VERSION}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
