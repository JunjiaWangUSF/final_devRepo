apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: frontend
    color: blue
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
