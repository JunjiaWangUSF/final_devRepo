apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-${COLOR}
  namespace: ${NAMESPACE}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-${COLOR}
      color: ${COLOR}
  template:
    metadata:
      labels:
        app: frontend-${COLOR}
        color: ${COLOR}
    spec:
      containers:
        - name: frontend
          image: ${IMAGE}
          ports:
            - containerPort: 3000
